<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta http-equiv="content-type" content="text/html; charset=UTF-8">
    <title><%= title %></title>
    <script src="/jquery"></script>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <header>
      <h1><%= title %></h1>
    </header>
    <div role="main">
      <table>
        <% for (var i in content) { %>
        <tr>
          <% var obj = content[i]; %>
          <th><%= obj.id %></th>
          <td><%= obj.name %></td>
          <td><%= obj.mail %></td>
          <td><%= obj.age %></td>
          <th><a href="/hello/show?id=<%= obj.id %>">詳細</a></th>
          <th><a href="/hello/edit?id=<%= obj.id %>">編集</a></th>
          <th><a href="/hello/delete?id=<%= obj.id %>">削除</a></th>
        </tr>
        <% } %>
      </table>
    </div>
  </body>
</html>
